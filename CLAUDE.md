# CLAUDE.md

هذا المسار هو المسار الرئيسي لمشروع Nesab dashboard.

المسار المحلي:
C:\Users\Admin\OneDrive\Desktop\Desktop-dashboard

الرابط الرسمي الحالي للداشبورد:
https://nesab-26771.web.app/

اقرأ أولاً:
- ./nesab_master_reference.md
- ./nesab_detailed_project_report.md

قواعد إلزامية:
- هذا المسار هو المرجع الرئيسي للداشبورد.
- الرابط الرسمي الحالي للداشبورد هو:
  https://nesab-26771.web.app/
- لا تنشئ أي رابط جديد للداشبورد.
- لا تنشئ أي مشروع Firebase جديد.
- لا تنشئ أي موقع Netlify جديد.
- لا تنشئ أي Hosting target جديد.
- لا تغيّر الرابط الرسمي الحالي أو مسار النشر الحالي إلا إذا طُلب ذلك صراحة.
- إذا كان الطلب متعلقاً بالداشبورد، فعدّل على المسار الحالي القائم فقط.
- لا تعتبر app الرسمي أو web الرسمي بديلاً عن dashboard.
- إذا كان الطلب قد يؤثر على web أو app، فاذكر ذلك أولاً قبل التنفيذ.
- Android&iOS مشروع منفصل مستقل ولا يُعدّل من هنا إلا إذا طُلب ذلك صراحة.

مسارات مساندة يمكن الرجوع لها عند الحاجة فقط:
- C:\Users\Admin\OneDrive\Desktop\Desktop-web
- C:\Users\Admin\OneDrive\Desktop\Desktop-app

قبل كل مهمة:
1. حدّد الملفات التي ستعدلها.
2. اذكر ما الذي لن تلمسه.
3. إذا احتاج التعديل رابطًا جديدًا أو نشرًا جديدًا أو بيئة جديدة، توقّف واسأل أولاً.

## مهم: تطبيق الداشبورد APK له نسختان (Native افتراضي + WebView خيار)

يوجد للداشبورد على الجوال **نقطتا دخول**، ولكل منهما سلوك مختلف. اختر حسب الحاجة:

### 1) النسخة Native (الافتراضية) — نقطة الدخول `lib/main.dart`
- تُرسم واجهة الداشبورد **محلياً داخل التطبيق** (مُجمّعة في الـAPK)، لا تعتمد على
  المتصفّح، وتعمل بلا إنترنت للواجهة (البيانات تأتي من Firestore).
- **لا تتزامن تلقائياً مع الويب:** أي تعديل على الواجهة لا يظهر إلا بعد **إعادة
  بناء الـAPK وتثبيته**.
- أمر البناء: `flutter build apk --release`  (يستخدم `lib/main.dart`).
- الناتج: `build/app/outputs/flutter-apk/app-release.apk`.

### 2) النسخة WebView (خيار) — نقطة الدخول `lib/main_webview.dart`
- غلاف رقيق يفتح الرابط الرسمي الحيّ `https://nesab-26771.web.app/` عبر WebView.
- **يتزامن تلقائياً مع الويب:** بعد تثبيتها مرة واحدة، أي تعديل يظهر بمجرد نشر
  الويب وإعادة فتح التطبيق — **بلا إعادة بناء**.
- أمر البناء: `flutter build apk --release -t lib/main_webview.dart`.
- تعتمد على حزمة `webview_flutter` (مضافة في `pubspec.yaml`)، ولا يستخدمها بناء
  الويب (`lib/main.dart`).

### قواعد مشتركة للنسختين
- **ابنِ خارج OneDrive** (مثل `C:\nesab_build`)؛ OneDrive يقفل ملفات البناء
  ويُفشِله.
- إصدار Flutter المطابق: **3.41.9 stable (Dart 3.11.5)** — يوافق `sdk: ^3.11.0`.
- التوقيع: كتلة release في `android/app/build.gradle.kts` توقّع بمفتاح **debug**
  (لا حاجة keystore).
- إضافة `google-services` **مُزالة عمداً** من `android/app/build.gradle.kts` لأن
  `google-services.json` مسجّل للحزمة `sa.nesab.app` (التطبيق الرسمي) لا الداشبورد.
  لا تُعِد تفعيلها.
- صلاحية الإنترنت موجودة في `android/app/src/main/AndroidManifest.xml`.
- **بيئة سحابية (Claude Code on the web):** بناء الـAPK يفشل لأن `dl.google.com`
  (Android SDK + Google Maven) محجوب بسياسة الشبكة. ابنِ محلياً على ويندوز أو عبر
  GitHub Actions (بيئتها تملك Android SDK).

### علاقته بالتطبيق الرسمي
- **لا علاقة لتطبيق الداشبورد إطلاقاً بتطبيق «نسب» الرسمي** (أندرويد/iOS) — مشروع
  منفصل تماماً، ولا يُعدَّل أحدهما من الآخر.

قاعدة إلزامية (سبب عطل سابق):
- عند إضافة/حذف قسم في الداشبورد حدّث **القائمتين معاً**: `side_menu.dart`
  (الكمبيوتر) و`mobile_layout.dart` (الجوال)، وكذلك `_buildContent` في
  `dahsboard_shell.dart`. نسيان قائمة الجوال يُخفي العناصر في تطبيق APK رغم
  ظهورها على الويب (الكمبيوتر).